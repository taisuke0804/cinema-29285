aaa

<% if Like.find_by(user_id: current_user.id, cinema_id: cinema.id) %>
  <%= link_to "いいね", cinema_unlike_path(cinema.id), method: :delete, remote: true,  class: "btn btn-default" %>
<% else %>
  <%= link_to "いいね", cinema_like_path(cinema.id), method: :post, remote: true, class: "btn btn-success" %>
<% end %>
<%= cinema.likes.length %>